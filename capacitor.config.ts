import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hmsapp.app',
  appName: 'HmsApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
